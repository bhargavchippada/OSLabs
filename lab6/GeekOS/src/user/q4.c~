#include <conio.h>
#include <sched.h>
int main(){
    int xyz;
    Get_NewToD(&xyz);
    Print("Time of Day: %d\n",xyz);
    int count;
    count = Get_Syscalls_Count();
    Get_NewToD(&xyz);
    Print("Time of Day: %d\n",xyz);
    Print("Number of syscalls: %d\n",count);
    return 0;
}
